@using PWA.Features.Sitemap.PageHeader.Offcanvas.Constants

@inject IJSRuntime JS

@code {
	protected override async Task OnAfterRenderAsync(bool firstRender)
	{
		if (firstRender)
		{
			await JS.InvokeVoidAsync("window.initializeDomIds", DomIds.Menu.OffCanvas);
		}
	}
}
